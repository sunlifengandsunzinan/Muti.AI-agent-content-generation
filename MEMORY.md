# MEMORY.md

## ⚠️ 基础设施（所有子Agent必读）

**shared/ 目录实际存储在 D:\shared\**
- 原路径 C:\Users\Administrator\.openclaw\workspace\shared\ 是符号链接（junction），指向 D:\shared\
- 所有子Agent用 shared/ 相对路径即可，无需改代码
- D盘剩余285GB

## 🚨 git 提交铁律（2026-08-05 血泪教训，详见 skill: douyin-git-commit-rules）

1. **大体积素材/工具（video-library/、capcut-mate/）绝对不进 git** — 曾导致 B_001 clip.mp4 170MB 卡死所有 push
2. **push 被拒 GH001 large files → 历史里有 >100MB blob，必须 filter-branch 清历史 + force push**
3. **⚠️ 清理历史时绝不先 stash --include-untracked 再 gc** — 2026-08-05 因此误删了未跟踪的 video-index.json / 封面方案 / diagnosis/plans/reports/TODO / material-24 素材源，全无法从git恢复
   - filter-branch 前要么先正常 commit 保证index干净，要么把重要未跟踪文件先拷到 .gitignore 外安全目录
   - 绝不 stash pop 之前跑 git gc --prune=now
4. **子Agent 正在写入的活数据文件（video-index.json / publish-log.json / data.json）不要擅自 stash/移动**
5. 清理后用 `git count-objects -vH` 验证 size-pack 很小（正常 <2MB）

## 📌 账号基础信息（所有技能必须参考）

**账号类型：** 体育类（跑步）
**视频画面构成原则：** 跑步实拍镜头占比 > 60%，非空镜为主
**账号定位：** 40岁普通人，慢跑时和自己说的话
**画面风格：** 真实跑步场景为主，空镜为辅，禁止AI感过重的画面

---

## 2026-07-29 子Agent调度教训
1. **不存在的子Agent不能甩锅给它** — 补采子Agent卡住后，就自己上手做，不能说是子Agent该做的
2. **子Agent卡住 → 查日志修复重试，不自己上手** — 子Agent卡住时应该检查根因（nodes.status失败？token不够？），修复后再重试，不能自己替它做完

## 抖音运营关键教训（持续更新）

### 2026-07-12 脚本生成三大教训

1. **观点重复检测** — 写任何脚本前必须先看最近1条已发布视频的核心观点。如果观点撞了，即使换素材/换场景/换角度也不行。宁可什么都不写，也不要复读。
   - 案例：011原生家庭版结尾"通往山顶的路不只一条"与"刚跑了两天"核心观点完全重复

2. **数据驱动选方向** — 用情报站采集的创作者中心数据+对标数据来决定"写什么"，不只是"怎么写"
   - 写前读：自账号TOP5开头方式 + 最低播放视频的坑 + 对标赛道爆款类型
   - 然后用这些数据**改**脚本结构，不只是列检查清单

3. **前10字必须有钩子** — "我跑了7年"这类陈述句开头=播放低保
   - 必须用：反问/悬念/冲突/数字/情绪词开头
   - 参考TOP5："那把镰刀，一开始我就没整明白"（悬念）、"40岁的人，一个月只有5个粉丝"（数字+自嘲）

### 子Agent任务设计教训
- 注册技能 ≠ 应用技能。子Agent启动时必须在任务指令中显式引用数据驱动技能
- 说"读数据"不够，要给具体范例和检查标准
- 三步检查必须强制改结构，不能只列清单

4. **先查最新数据，再做事** — 每次写脚本前必须读creatorCenter最近数据，找出「涨」的趋势
   - 不是随意选素材，必须用最新数据决定方向
   - 素材不够匹配涨的趋势 → 通知素材管家要新素材，不要硬写

5. **结尾必须选已验证有效的类型** — 4种有效：没整明白式/你发现没式/两面选择式/自嘲接话式
   - 禁止硬诱导、格言型、抽象大词型、总结式
   - 结尾必须自然，单条只用一种类型

6. **对标数据质量过滤** — 对标原型播放<500不可用，500-2000标注低播放参考
   - 2026-07-17案例：凑整强迫症参考了591播的「去跑步出门前」，但该视频自己都没跑过第一波推荐
   - 已在脚本生成器SKILL中注册为铁律10

7. **低播放视频自动分析** — 采集时对发布>24h且播放<300的视频自动分析原因
   - 2026-07-17案例：凑整强迫症28h仅251播，死因为定时发布故障
   - 已在创作者中心SKILL中注册为第七步

### 2026-07-29 脚本生成器结构质量教训

1. **SKILL必须定义"好的脚本长什么样"的标准** — 只写"结尾4种类型"不够，子Agent不知道什么是好的中间衔接、什么是好的结尾。新增：脚本结构模板（钩子→呼应→过渡→升华→收束）+ 结尾质量标尺（情绪收束≠叙事收束）
   - 案例：2026-07-29 material-24脚本，中间断层（问句直接跳结论）、结尾变叙事（"跑一圈就没那么憋着了"），子Agent自己看不出问题

2. **禁止脚本生成器跨职责输出标题/分镜** — 标题是标题封面官的活，分镜是jm子Agent的活。已在SKILL第4步顶部新增显式禁止字段

3. **启动子Agent时不要传全量data.json文本** — 
   - ❌ 错误做法：把整个data.json（对标库60条+素材+创作者中心数据）塞进任务指令，导致情报站诊断一次消耗72.9K in tokens
   - ✅ 正确做法：只传必需摘要，子Agent需要全量数据时自己去 read shared/data.json

4. **脚本生成器产出不通过应持续打回重写** — 
   - ❌ 错误做法：脚本生成器第二版不够好就直接让峰峰自己写
   - ✅ 正确做法：指出具体问题，打回重写，直到自检清单全部通过且峰峰认可，不替峰峰写脚本

### 2026-07-27 最省Token铁则注册

**核心原则：Token > 时间 > 完美。**

已注册到：
- SOUL.md (硬规则段)
- AGENTS.md (全局需知段)
- douyin-脚本生成器 SKILL.md
- douyin-情报站 SKILL.md
- douyin-标题封面官 SKILL.md

具体规则：
1. 浏览器操作卡壳 → 切方案让用户自己操作，不跟UI死磕
2. 批量AI生成 → 一次性写prompt列表，用户App里生成
3. 子Agent调度不传全量data.json文本
4. 模型不支持的能力 → 直接说替代方案

案例：2026-07-27在即梦网页版上折腾弹窗/比例设置/发送按钮花了大量token，最终只需要告诉用户

**教训：精选视频不能做成建议向/教程向/清单向。**

**案例：**
- 2026-07-21峰峰口述

## 🚨 精选视频准则（历史教训，必须记住）
精选池和自然推荐池是两个独立流量系统。日常播放低不意味着不能发精选。禁止用日常口播数据判断精选视频可发性。

已注册到：SOUL.md（硬规则段）、AGENTS.md（全局需知段）、所有子Agent SKILL.md和agent.md

历史错误记录（2026-07-27，峰峰指出两次）：
1. 情报站评估：用日常口播15秒爆款标准量90秒精选视频
2. 本会话：用基础播放低判断不适合发精选
两次都是对精选池流量机制的根本误解，已经修复。

### 2026-07-27 内容发布策略：80/20法则
- 口播日常（80%）：保持更新频率，养基础播放，跑量为主
- 精选视频（20%）：脚本+素材到位时冲，发完回归日常
- 两者不冲突，精选发完第二天正常发日常
- 已注册到所有子Agent的agent.md和SKILL.md
